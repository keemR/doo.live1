---
import { format } from 'date-fns';

interface Props {
  date: Date;
  class?: string;
}

const { date, class: className = '' } = Astro.props;
---

<time datetime={date.toISOString()} class:list={["text-sm text-gray-500", className]}>
  {format(date, 'MMM d, yyyy')}
</time>